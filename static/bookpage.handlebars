<!DOCTYPE html> 
<html lang ="en">
	<head> 
		<title>A Book</title>
	</head>

	<body> 
		<h1>{{title}}</h1>
		<img src="{{coverSource}}"> 
		<p>{{title}}</p>
		<p>{{author}}</p>
		<p>{{description}}</p>

	</body>

</html> <!--the last --> 